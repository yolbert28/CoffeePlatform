CREATE TABLE loan_status (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

selectAll:
SELECT * FROM loan_status;

-- CAMBIO: Renombramos la función
insertNew:
INSERT INTO loan_status(name) VALUES (?);